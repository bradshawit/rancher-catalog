deluge:
  image: bradshawit/docker-deluge:latest
  command: 
  expose: 
   - ${public_port}:8112
  environment:
    CLOUDFLARE_EMAIL: ${CLOUDFLARE_EMAIL}
    CLOUDFLARE_KEY: ${CLOUDFLARE_KEY}
    ROOT_DOMAIN: ${ROOT_DOMAIN}
    TTL: ${TTL}
